// Game scaler module - placeholder for egui port
// The pixels-based GameScaler has been removed; egui handles scaling natively
#[derive(Default)]
pub struct GameScaler;

impl GameScaler {
    pub fn new() -> Self {
        Self
    }
}
