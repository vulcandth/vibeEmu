[package]
name = "vibe-emu-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
apu-trace = []

[dev-dependencies]
once_cell = "1"
tempfile = "3"
reqwest = { version = "0.12", features = ["blocking", "gzip"] }
zip = "4.3"
png = "0.17"
